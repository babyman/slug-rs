val build = fn(remaining, snapshots, value) {
    if (remaining == 0) {
        snapshots
    } else {
        recur(remaining - 1, snapshots :+ value, value copy { item: remaining })
    }
}

build(128, [], {})
